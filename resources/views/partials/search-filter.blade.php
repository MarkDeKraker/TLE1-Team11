<!-- <form method="GET" action="{{route('home')}}">
    <div class="fixed top-34 bg-white w-full"> -->

        <!-- SEARCHBAR -->
        <!-- <section>
            <div class="searchbar m-3">
                <label class="w-full">
                    <input class="block w-full p-3 ps-10 border border-orange-400 border-1 rounded-full text-xl"
                        type="text" placeholder="Zoeken naar titels en beschrijvingen..." name="search">
                </label>
            </div>
        </section>

        <div id="overlay" class="overlay"></div>
        <div id="userModal" class="modal">
            <div class="modal-content">
                <span class="close" id="closeModal"><img src="icons/close.png" /></span>
                <form class="filter-form" action="#" method="get">
                    <div class="filter-subject">
                        @foreach($subjects as $subject)
                        <div class="filter-title">Categorie</div>
                        <label>
                            <input class="sub-btn" type="checkbox" name="subjects[]" value="{{$subject->id}}">
                            {{$subject->subject}}
                        </label>
                        @endforeach
                    </div>

                    <div class="filter-age">
                    @foreach($ages as $age)
                        <div class="filter-title">Leeftijd</div>
                        <label>
                            <input type="checkbox" name="age[]" value="14-16">{{$age->age}} Jaar
                        </label>
                        @endforeach
                    </div>
                    <div class="filter-btn">
                        <input type="submit" value="filters toepassen" src="icons/close.png">
                        <a href="{{ route('home') }}" class="">Verwijder Filters</a>
                    </div>
                </form>
            </div>
        </div> -->
    

        